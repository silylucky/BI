# <项目名> — 产品需求文档（PRD · Hub）

```yaml
version: 1.0.0
last_updated: <YYYY-MM-DD>
truth_source: true
evolution_hub: true   # G2 选题只读本文件；明细按需读 prd/*.md
goal_ref: docs/automate/goal.md
features_ref: docs/automate/prd/
scoring_ref: .cursor/automate/skills/prd-scoring/SKILL.md
```

> **本文件是索引层（hub）**：8 维评分、薄弱项汇总、功能索引**仅在本文件**维护。
> 验收标准、代码锚点、逐条说明在 [`prd/`](./prd/) 分片，按 ID 按需加载以节省 token。

<!-- 反漂移硬规则（所有 agent 必须遵守）：
1. 「系统薄弱项汇总」只反映当前状态；禁止在该区追加历史性 note / blockquote，每轮变更只写入修订记录
2. 功能状态单点维护于 prd/ 分片；hub 不设独立 Backlog 状态表
3. 修订记录只保留最近 10 条，更早条目直接删除（git 历史即归档）
4. 状态枚举统一：未实现 · 部分实现 · 已实现 · 已废弃（须注明替代）
5. 每个功能项在分片中必须有 goal_ref 字段（指向 goal.md 终极目标编号）
-->

## 系统薄弱项汇总（按总分升序，供选题）

> 更新：<YYYY-MM-DD> · 规范见 `prd-scoring` · 只保留当前状态，禁止堆积历史

| 排名 | ID | 功能 | 总分 | 最薄弱维度 | 建议优先级 |
|------|-----|------|------|------------|------------|
| 1 | | | | | 高 |

---

## 功能项 8 维评分总表

> 各列存**维度分%**（0–100，N/A 标 `N/A`）；末列 **加权总分**（高×3：用户价值/完整度/可靠性，中×2：其余五维；公式见 prd-scoring）。
> 重评须附证据（见 prd-scoring）；禁止整行同分敷衍。

| ID | 用户价值 | 完整度 | 可靠性 | 交互体验 | 架构健康 | 测试覆盖 | 性能 | 安全性 | 加权总分 | 薄弱项 |
|----|:--------:|:------:|:------:|:--------:|:--------:|:--------:|:----:|:------:|:--------:|--------|
| | | | | | | | | | | |

---

## 功能索引（明细见 `prd/` 分片）

> **G2 选题**：只读本文件（hub），**禁止加载分片**。
> **P1 / P3 / P5**：按入选 ID 只读对应分片单文件。

| 分片 | 功能 ID 域 | 明细路径 |
|------|------------|----------|
| F01-<域>.md | <ID 前缀>-001 ~ | `prd/F01-<域>.md` |

---

## 里程碑

里程碑见 `docs/automate/plan.md`（人工维护，create-evolution-plan）；不存在时本节留空。

---

## 修订记录（最近 10 条）

| 版本 | 日期 | 说明 |
|------|------|------|
| 1.0.0 | <YYYY-MM-DD> | 初版 |
