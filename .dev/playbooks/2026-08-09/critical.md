# 走查剧本 · VitalSpan 后台管理 · 首租户主路径

| 项 | 内容 |
|----|------|
| mode | specified |
| created | 2026-08-09 |
| source | scenario-playbook |
| blueprint | `docs/material/blueprints/2026-08-09-system-admin-audit.md`（F1～F4，用户已确认） |
| .dev | 有 · `.dev/config.yaml` |
| allow_writes | true |
| allow_destructive | false |
| handoff | browser-reviewer |

## 场景索引

| 场景 ID | 标题 | 优先级 | 角色 | 写操作 | 状态 |
|---------|------|--------|------|--------|------|
| S0 | 预检与健康检查 | P0 | — | 否 | ready |
| S1 | 登录并进入配置向导 | P0 | admin | 否 | ready |
| S2 | 组织架构维护 | P0 | admin | needs_confirm | ready |
| S3 | 岗位角色与权限矩阵 | P0 | admin | needs_confirm | ready |
| S4 | 用户与角色/组织绑定 | P0 | admin | needs_confirm | ready |
| S5 | 资源授权 | P1 | admin | needs_confirm | ready |
| S6 | 侧栏导航单项高亮 | P1 | admin | 否 | ready |

## 完整闭环夹具（仅 `allow_writes=true` 时使用）

| 实体 | 示例值 | 说明 |
|------|--------|------|
| 组织名称 | `演示综合处` | 上级选「（顶级组织）」 |
| 角色编码 | `demo_analyst` | 显示名：`演示分析员` |
| 权限 | 勾选「数据看板」相关读权限 | 在编辑角色 →「权限」Tab →「保存权限」 |
| 用户名 | `walkthrough_analyst` | 初始密码：`Walkthrough1`（≥8 字符） |
| 资源授权 | 角色=`演示分析员`，类型=`仪表板`，资源=列表第一项 | 按环境已有仪表板选择 |

## 共享前置（全场景默认）

| ID | 步骤 | 路由/入口 | 操作 | 期望 | 截图 | Console |
|----|------|-----------|------|------|------|---------|
| G0.1 | API 健康 | `http://localhost:8000/health` | GET（curl 或预检） | HTTP 200 | 否 | — |
| G0.2 | 打开前端 | `base_url` → `http://localhost:5173/admin` | 导航 | 页面加载；非连接拒绝/白屏 | 是 | 查 |
| G0.3 | 登录 | `/login` | 用户名 `admin`；密码读 `VITALSPAN_DEV_ADMIN_PASSWORD` | 进入 `/admin` 壳；顶栏可见 | 是 | 查 |
| G0.4 | 登录表单 | `/login` | 点击「登录」 | 按钮文案「登录中…」后跳转；无「用户名或密码错误」 | 是 | 查 |

---

## 场景 S0 · 预检与健康检查

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 业务价值 | 走查前确认依赖服务可用，避免假失败 |
| 角色 | 无 |
| 前置夹具 | docker compose + backend + fe dev 已启动 |
| 关联 | `.dev/config.yaml` `environments.local` |
| 写操作 | none |

### 步骤

| 步 | 入口 | 操作 | 期望 | 失败貌 | 截图 | Console |
|----|------|------|------|--------|------|---------|
| 1 | `health_url` | 请求 `http://localhost:8000/health` | JSON/文本表明服务存活 | 连接拒绝/5xx | 否 | — |
| 2 | `browser_url` | 打开 `http://localhost:5173/login` | 登录页标题区含「使用管理员账号登录 VitalSpan 管理后台」 | 白屏/Vite 报错 | 是 | 查 |

### 通过判据

- [ ] health 200
- [ ] 登录页可渲染

---

## 场景 S1 · 登录并进入配置向导

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 业务价值 | 验证管理员能找到后台入口，向导进度与四步卡片可信（F1） |
| 角色 | admin |
| 前置夹具 | S0 通过 |
| 关联 | `SystemAdminHomePage.tsx` · `system-admin-nav.tsx` · PRD AUTH-001 |
| 写操作 | none |

### 步骤

| 步 | 入口（菜单文案或路由） | 操作 | 期望（可见结果/状态） | 失败貌 | 截图 | Console |
|----|------------------------|------|----------------------|--------|------|---------|
| 1 | `/login` | 填写「用户名」「密码」→「登录」 | 进入工作台壳层（URL 含 `/admin`） | 停留在登录页 | 是 | 查 |
| 2 | 顶栏 | 点击 `aria-label="系统管理"` 的齿轮链接 | 导航至 `/admin/system`；顶栏齿轮消失（已在后台区） | 404/无入口 | 是 | 查 |
| 3 | 页头 | — | 标题「后台管理」；描述含「按步骤完成人员、组织与权限配置」 | 错页 | 是 | — |
| 4 | 向导区 | — | 小标题「首租户配置向导」；下方四步卡片：「建立组织架构」「配置岗位角色」「开通用户账号」「分配可访问资源（可选）」 | 永久 Skeleton | 是 | — |
| 5 | 侧栏 | — | 仅「配置向导」一项高亮（`/admin/system`）；「角色管理」等同屏不高亮 | 两项同时 active | 是 | — |
| 6 | 步骤卡片 | 点击「建立组织架构」链接 | 进入 `/admin/system/orgs`；侧栏「组织架构」高亮、「配置向导」不高亮 | 路由错误 | 是 | 查 |
| 7 | 侧栏「配置向导」 | 点击返回 | 回到 `/admin/system` | — | 是 | — |
| 8 | 快捷入口 | 侧栏右栏「快捷入口」→「用户管理」 | 进入 `/admin/system/users` | — | 是 | — |
| 9 | 头像菜单（备选入口） | 先 `returnToWorkspace` 回工作台 → 点头像 →「后台管理」 | 再次进入 `/admin/system` | 菜单无「后台管理」 | 是 | 查 |

### 通过判据

- [ ] 双入口（顶栏齿轮 / 头像「后台管理」）至少一条可达
- [ ] 向导四步文案与进度区可见
- [ ] 侧栏最长路径匹配：子页 active 时「配置向导」不双高亮

### 跳过规则

- 无

---

## 场景 S2 · 组织架构维护

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 业务价值 | 首租户第一步：可建组织树（F2） |
| 角色 | admin |
| 前置夹具 | 无（空树亦可） |
| 关联 | `OrgTreePage.tsx` · `POST /api/v1/orgs` |
| 写操作 | needs_confirm |

### 步骤

| 步 | 入口 | 操作 | 期望 | 失败貌 | 截图 | Console |
|----|------|------|------|--------|------|---------|
| 1 | 侧栏「组织架构」`/admin/system/orgs` | 导航 | 标题「组织架构」；空态或组织树 | 白屏 | 是 | 查 |
| 2 | 页头 | 点击「新建组织」 | 对话框标题「新建组织」 | 无响应 | 是 | — |
| 3 | 表单 | 「名称」填 `演示综合处`；「上级组织」选「（顶级组织）」 | 「创建」按钮可点（名称非空） | 校验不明确 | 否 | — |
| 4a | `allow_writes=false` | 点击「取消」 | 对话框关闭；树无新增（或保持原状） | 误提交 | 是 | 查 |
| 4b | `allow_writes=true` | 点击「创建」 | toast 成功；树出现「演示综合处」；向导「组织节点」计数 ≥1 | 静默失败/5xx | 是 | 查 |

### 通过判据

- [ ] 新建对话框字段与按钮文案正确
- [ ] 只读模式：未提交创建
- [ ] 写模式：组织出现在树中

---

## 场景 S3 · 岗位角色与权限矩阵

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 业务价值 | 定义岗位功能权限（F3/F4 前置） |
| 角色 | admin |
| 前置夹具 | 无 |
| 关联 | `RoleListPage.tsx` · `RolePermissionsPanel.tsx` |
| 写操作 | needs_confirm |

### 步骤

| 步 | 入口 | 操作 | 期望 | 失败貌 | 截图 | Console |
|----|------|------|------|--------|------|---------|
| 1 | 侧栏「人员与权限」→「角色管理」`/admin/system/roles` | 导航 | 标题「角色管理」；列表含内置角色 | 白屏 | 是 | 查 |
| 2 | 页头 | 点击「新建角色」 | 对话框「新建角色」 | — | 是 | — |
| 3 | 表单 | 「角色编码」=`demo_analyst`；「显示名」=`演示分析员`；「描述」可填 `走查用` | 「保存」可点 | 字段错误无提示 | 否 | — |
| 4a | `allow_writes=false` | 点击「取消」 | 对话框关闭 | 误保存 | 是 | — |
| 4b | `allow_writes=true` | 点击「保存」 | 列表出现新角色；对话框关闭 | 5xx | 是 | 查 |
| 5 | 列表行 | 点击该角色「编辑」（或等价入口） | 对话框「编辑角色」；Tab「基本信息」「权限」 | — | 是 | — |
| 6 | Tab「权限」 | 展开「数据看板」等分组；勾选至少一项读权限 | 「保存权限」可点 | 矩阵空白不可操作 | 否 | — |
| 7a | `allow_writes=false` | 不点「保存权限」；点「关闭」 | 离开编辑态 | — | 是 | — |
| 7b | `allow_writes=true` | 点击「保存权限」 | toast 成功 | 静默失败 | 是 | 查 |

### 通过判据

- [ ] 新建/编辑/权限 Tab 可达
- [ ] 只读模式未持久化变更

---

## 场景 S4 · 用户与角色/组织绑定

| 项 | 内容 |
|----|------|
| 优先级 | P0 |
| 业务价值 | 开通业务账号并绑定岗位与组织（F3） |
| 角色 | admin |
| 前置夹具 | S2 有组织、S3 有业务角色（写模式）；只读模式可用已有数据 |
| 关联 | `UserListPage.tsx` · `CreateUserDialog.tsx` · `UserManageSheet.tsx` |
| 写操作 | needs_confirm |

### 步骤

| 步 | 入口 | 操作 | 期望 | 失败貌 | 截图 | Console |
|----|------|------|------|--------|------|---------|
| 1 | 侧栏「用户管理」`/admin/system/users` | 导航 | 标题「用户管理」 | 白屏 | 是 | 查 |
| 2 | 页头 | 点击「创建用户」 | 对话框「创建用户」 | — | 是 | — |
| 3 | 表单 | 「用户名」=`walkthrough_analyst`；「初始密码」=`Walkthrough1`（或点「生成随机密码」） | 「创建」可点 | 密码长度提示 | 否 | — |
| 4a | `allow_writes=false` | 点击「取消」 | 对话框关闭 | 误创建 | 是 | — |
| 4b | `allow_writes=true` | 点击「创建」 | Alert「用户已创建」含用户名与密码 | 5xx | 是 | 查 |
| 5 | 列表 | 对新用户点击「管理」 | Sheet「管理用户 — walkthrough_analyst」；Tab「角色」「组织与安全」 | — | 是 | — |
| 6 | Tab「角色」 | 勾选 S3 创建的「演示分析员」 | 「保存角色绑定」可点 | 无角色列表 | 否 | — |
| 7a | `allow_writes=false` | 关闭 Sheet，不保存 | — | — | 是 | — |
| 7b | `allow_writes=true` | 点击「保存角色绑定」 | toast「角色已更新」 | — | 是 | 查 |
| 8 | Tab「组织与安全」 | 「选择组织」选 S2 组织 →「保存组织归属」 | 写模式：toast「组织归属已更新」；只读：仅验证控件存在后关闭 | — | 是 | 查 |
| 9 | `/admin/system` | 返回配置向导 | 「开通用户账号」步显示完成（绿勾）；计数排除当前 admin | 仅 admin 时仍绿 | 是 | — |

### 通过判据

- [ ] 创建用户表单与 Sheet 双 Tab 可达
- [ ] 写模式：向导用户步 done 逻辑正确

---

## 场景 S5 · 资源授权

| 项 | 内容 |
|----|------|
| 优先级 | P1 |
| 业务价值 | 限定角色可见仪表板/报表/数据源（F4，向导标注可选） |
| 角色 | admin |
| 前置夹具 | 至少一个业务角色；环境中有可授权资源 |
| 关联 | `GrantsPage.tsx` · `POST /api/v1/resource-grants` |
| 写操作 | needs_confirm |

### 步骤

| 步 | 入口 | 操作 | 期望 | 失败貌 | 截图 | Console |
|----|------|------|------|--------|------|---------|
| 1 | 侧栏「资源授权」`/admin/system/grants` | 导航 | 标题「资源授权」 | 白屏 | 是 | 查 |
| 2 | 页头 | 点击「新建授权」 | 对话框「新建授权」 | — | 是 | — |
| 3 | 表单 | 「角色」选 `演示分析员`；「资源类型」选「仪表板」；「资源」选列表第一项 | 「确认」可点 | 资源列表空 | 否 | — |
| 4a | `allow_writes=false` | 点击「取消」 | 对话框关闭 | 误提交 | 是 | — |
| 4b | `allow_writes=true` | 点击「确认」 | 列表出现新授权行 | 5xx | 是 | 查 |

### 通过判据

- [ ] 三类资源类型标签：数据源 / 仪表板 / 报表
- [ ] 只读模式未新增授权

---

## 场景 S6 · 侧栏导航单项高亮

| 项 | 内容 |
|----|------|
| 优先级 | P1 |
| 业务价值 | 回归 2026-08-09 修复：子路由不应与「配置向导」双高亮 |
| 角色 | admin |
| 前置夹具 | 无 |
| 关联 | `fe/src/lib/nav-active.ts` · `app-sidebar.tsx` |
| 写操作 | none |

### 步骤

| 步 | 入口 | 操作 | 期望 | 失败貌 | 截图 | Console |
|----|------|------|------|--------|------|---------|
| 1 | `/admin/system/roles` | 直接导航 | 侧栏「角色管理」active；「配置向导」**不** active | 两项同时高亮 | 是 | — |
| 2 | `/admin/system` | 导航 | 仅「配置向导」active | — | 是 | — |
| 3 | `/admin/system/users` | 导航 | 「用户管理」active（父组「人员与权限」展开） | — | 是 | — |

### 通过判据

- [ ] 最长路径匹配：任意 system 子页与 `/admin/system` 不双亮

---

## 全局通过判据

- [ ] 所有 `ready` 场景步骤期望满足
- [ ] 过程无未捕获 console error（已知第三方白名单外）
- [ ] 主路径无意外 5xx

## 全局跳过规则

- `allow_writes=false` → 所有标 `4a/7a` 分支执行，跳过 `4b/7b` 提交步
- `allow_destructive=false` → 删除/撤销/停用仅开确认框，不点确认（见 optional.md）
