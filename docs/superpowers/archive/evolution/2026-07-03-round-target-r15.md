# 演化轮次选题 — 2026-07-03（M1 BOOT quality push r15）

## 本轮演化目标（共 5 项）

### 选题决策

- **批量主题**：M1 工程基线 — **BOOT quality push r15**（r14 已将 BOOT-003/005/006/002/001 推至 90.0–90.4；本轮主攻 **唯一未过线 BOOT-004(89.5)** 并巩固刚破线的 **BOOT-005/006(90.0 地板)** 与薄弱维仍偏低的 **BOOT-002/003**）
- **来源**：`docs/automate/plan.md` §M1 + §M1B 勾选 **12/12 已完成**，无活跃 `[ ]` 里程碑节；`prd.md` hub 8 维 — **已实现未过线唯一项** BOOT-004(89.5)；`git log -5` 显示 r14 已合并（PR #31，dev-auto@43cbf98），五 BOOT 项破 90；`evolution-state.md` 待办池空
- **合并理由**：饱和熔断未触发（plan 无未完成项故执行熔断检查；Top5 薄弱汇总 META-001~CONN-009 均为远期未实现 10.8–11.1，均 <90，非评分饱和；待办池无未消化项）；r14 清除 BOOT STUCK 五 ID，仅 **BOOT-004 连续 10 轮未过 90**；同属 M1 `core`/`auth`/`fe` 壳层域，可批处理配置/日志/鉴权/CI smoke，对齐 goal G1 工程基线
- **范围框定**：
  - **模块**（3）：`backend/app/core/`、`backend/app/auth/`、`fe/src/`（壳层与 layout）、`tests/`（含 `fe/` vitest）
  - **文件**（合计约 16，≤20）：各子项所列测试、fixture、CI 守卫；**不修改** `goal.md` / `plan.md` 结构
  - **不含**：DATA/ETL（M1B 全簇 ≥90.6）；远期 META/DESIGN/CONN 未实现项；M2+ AUTH-001 等新功能立项
- **不足 5 项原因**：不适用 — 本轮满 5 项，均为 M1 BOOT 推分/巩固同批主题

### 候选对比

| 候选 prd ID | 加权总分 | 未选原因 |
|-------------|:--------:|----------|
| META-001 | 10.8 | F11-META 术语字典，M11+ 远期，完整度 5% 因未实现 |
| DESIGN-001 | 10.8 | F12-DESIGN 拖拽查询，M10+ 治理设计器 |
| CONN-021 | 10.9 | TiDB 连接器属 M4，非 M1 |
| BOOT-001 | 90.4 | r14 已破 90 且为 BOOT 簇最高分，本轮让位地板项与 BOOT-004 |
| DATA-004 | 91.1 | M1B 全簇 ≥90.6，非当期薄弱 |
| AUTH-001 | 13.2 | M2 未立项，完整度 5% 因未实现 |

### STUCK 标注（连续未过 90，本轮入选）

- **STUCK: BOOT-004 连续 10 轮未过 90**（最近 89.5）— 距 90 仅 0.5 分，本轮主攻突破；建议人工 `create-evolution-plan` 复核 BOOT-004 验收阈值（配置/日志/trace）若 r15 仍不过线

---

### 子项 1：BOOT-004 配置与日志基线

- **选题理由**：hub **已实现未过线唯一项 89.5**；**用户价值 80%** 仍为主薄弱维；r14 未入选本轮 rotation，连续 10 轮 STUCK；主攻 **破 90 线** — 扩 `Settings` 边界校验、结构化日志字段契约、`TraceIdMiddleware` 透传/降级与 `LOG_LEVEL` 动态切换 L1 断言
- **选题时 PRD 加权总分**：89.5/100（用户价值 **80%** · 完整度 98% · 可靠性 88% · 架构 90% · 测试覆盖 98% · 性能 **86%** · 安全性 **88%** · 交互 N/A）
- **主攻薄弱维**：用户价值（80%→≥84%）；性能（86%→≥88%）
- **用户感知**：配置错误在启动/CI 可早期发现；请求日志含一致 `traceId` 便于排障
- **类型**：补缺（配置边界 + 日志/trace 契约 smoke）
- **验收标准**（来源 plan §BOOT-004）：
  - 扩展 `tests/`：`Settings` 非法/缺失/越界 env；`TraceIdMiddleware` 无头/重复头/超长头；`LOG_LEVEL` 切换后日志级别断言
  - `GET /health` 后 JSON 日志含 `traceId`；pytest 全绿

### 子项 2：BOOT-005 数据库迁移框架

- **选题理由**：加权总分 **90.0**（r14 刚破线地板）；**用户价值 82%**、**性能 86%** 仍偏低；巩固过线成果 — 扩 Alembic revision 链完整性、离线/在线迁移 smoke 与 `DATABASE_URL` 连接池超时边界
- **选题时 PRD 加权总分**：90.0/100（用户价值 **82%** · 完整度 96% · 可靠性 90% · 架构 88% · 测试覆盖 100% · 性能 **86%** · 安全性 90% · 交互 N/A）
- **主攻薄弱维**：用户价值（82%→≥84%）；性能（86%→≥88%）
- **用户感知**：迁移失败有结构化错误；本地 compose 元库连接异常可早期捕获
- **类型**：补缺（迁移链 + 连接性能 smoke）
- **验收标准**（来源 plan §BOOT-005）：
  - 扩展 `tests/`：revision 链 head 一致性；`alembic upgrade`/`downgrade` smoke；`DATABASE_URL` 超时/非法连接断言
  - pytest 全绿

### 子项 3：BOOT-006 CI 与质量门禁

- **选题理由**：加权总分 **90.0**（r14 刚破线地板）；**安全性 86%**、**性能 86%** 仍为主薄弱维；巩固 CI 收集率地板、ruff/pytest/vitest 并行超时与 workflow 缓存失效边界
- **选题时 PRD 加权总分**：90.0/100（用户价值 84% · 完整度 96% · 可靠性 90% · 架构 88% · 测试覆盖 100% · 性能 **86%** · 安全性 **86%** · 交互 N/A）
- **主攻薄弱维**：安全性（86%→≥88%）；性能（86%→≥88%）
- **用户感知**：PR CI 在异常 workflow/依赖缺失时失败信息可定位；质量门禁稳定不 flaky
- **类型**：补缺（CI 守卫 + 安全/性能 smoke）
- **验收标准**（来源 plan §BOOT-006）：
  - 扩展 `tests/` 与 `.github/workflows/ci.yml` 契约测试：pytest 收集率地板；ruff 违规样例断言；frontend job 超时边界 smoke
  - `ruff check` + `pytest` + `pnpm test` + `build` + `check:design` 全绿

### 子项 4：BOOT-002 React 管理端壳层

- **选题理由**：加权总分 **90.1**；**安全性 84%** 为 BOOT 簇最低；r14 已扩 Tab focus smoke，本轮可补路由守卫、主题 Token 越界与 `check:design` 违规样例 FE 安全/无障碍断言
- **选题时 PRD 加权总分**：90.1/100（用户价值 84% · 完整度 96% · 可靠性 88% · 交互体验 94% · 架构 92% · 测试覆盖 98% · 性能 86% · 安全性 **84%**）
- **主攻薄弱维**：安全性（84%→≥88%）；性能（86%→≥90% 维度分）
- **用户感知**：Admin 壳层在非法路由/主题切换下不泄露敏感状态；无障碍焦点遍历在 CI 可验证
- **类型**：补缺（FE 安全 smoke + a11y 巩固）
- **验收标准**（来源 plan §BOOT-002）：
  - `fe/` vitest：非法 `/admin/*` 路由重定向；主题 Token 缺失降级；键盘焦点顺序 smoke
  - `pnpm test` + `build` + `check:design` 全绿

### 子项 5：BOOT-003 鉴权中间件骨架

- **选题理由**：加权总分 **90.1**；**用户价值 82%** 仍偏低；r14 已扩 `/me` 与 Bearer 边界，本轮可补 `PUBLIC_PATHS` 前缀匹配、多 scheme 混用与 `get_current_user` 依赖注入异常路径 L1 断言
- **选题时 PRD 加权总分**：90.1/100（用户价值 **82%** · 完整度 100% · 可靠性 90% · 架构 88% · 测试覆盖 94% · 性能 86% · 安全性 90% · 交互 N/A）
- **主攻薄弱维**：用户价值（82%→≥84%）；测试覆盖（94%→≥98%）
- **用户感知**：未授权访问 `/api/v1/me` 返回一致 401；公开路径豁免行为在 CI 可回归
- **类型**：补缺（鉴权边界 + 公开路径契约）
- **验收标准**（来源 plan §BOOT-003）：
  - 扩展 `tests/`：`PUBLIC_PATHS` 边界（`/health` vs `/healthz`）；Bearer 空/畸形/过期占位；`GET /api/v1/me` 全分支回归
  - pytest 全绿
