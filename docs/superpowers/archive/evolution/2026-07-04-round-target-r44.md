# 本轮演化目标（共 5 项）— r44

> 生成：2026-07-04 · G2 evolution-picker · 来源：prd.md hub 8 维评分（薄弱项汇总）
> 主题：**M8/M12/M13 集成 API L1 kickoff — API 域**

## 选题决策

- **批量主题**：集成 API L1 kickoff（IF-01~04 查询/总线/报表/嵌入 + OpenAPI 版本策略骨架）
- **来源**：`docs/automate/prd.md` hub「系统薄弱项汇总」+「8 维评分总表」（`docs/automate/plan.md` M1/M1B 全勾选、无活跃 `[ ]` 行 —— 已知 concern，本轮回落纯 8 维选题）；`git log -5` r43 PR #69 已合并（VIZ companion 质量推分）
- **合并理由**：F13-API 域 5 个远期集成项均 `未实现`（完整度 5%、可靠性/测试/性能 0%），加权总分 11.3–13.1 同一量级、同一分片、同一交付形态（REST 契约 + 错误域 + smoke 测试）。取全部 5 项成批，同时捕获**全表绝对最低分** API-003(11.3)，符合「加权总分最低优先 + 同主题批处理 + ≤3 模块」批量规则。API-001/002 已在 r30/r31 推至 ≥90，本轮不重复。
- **范围框定**：L1 kickoff（首轮骨架推分，对齐 r36/r38/r40/r42 连接器/VIZ L1 kickoff 模式）——backend 集成 API 路由/schema + 错误域 + auth 守卫 smoke + pytest；估算 ≤20 文件、≤3 模块（`backend/app/api/v1/` 集成路由簇 + 可能 `backend/app/integration/` 或治理域薄编排 + tests）。不追求一次到 90，目标完整度/可靠性/测试从 0–5% 抬升至可用骨架档位。
- **不足 5 项原因**：不适用（已满 5 项；F13-API 域未实现项恰好 5 个）。
- **STUCK**：`evolution-state.md`「选题卡住计数」表为空；5 项均首次入选，无 STUCK 标注。
- **饱和熔断**：未触发（plan 无未完成 `[ ]` 故执行熔断检查；Top5 薄弱汇总 API-003/NFR-006/007 等为 11.3–11.5 « 90，非评分饱和；待办池无未消化项）。

## 候选对比

| 候选 prd ID | 加权总分 | 未选原因 |
|-------------|:--------:|----------|
| API-003 IF-02 查询服务 API | 11.3 | **入选**（全表最低） |
| NFR-006 浏览器与消息推送 | 11.4 | NFR 横切域，与 API 集成模块内聚性弱，另立 NFR 批 |
| NFR-007 信创国产化 | 11.4 | 同上；M13 信创项留后续与 CONN 信创连接器同批更合适 |
| GOV-005 查询服务发布 | 11.5 | 治理域 FR-1.4，与 API-003 业务相邻但模块不同；API 五件套优先闭合 IF 契约 |
| NFR-005 连接器插件扩展性 | 11.5 | NFR 横切，非集成 API 路由簇 |
| API-005 IF-03 报表文档 API | 11.6 | **入选**（API 域同主题） |
| API-004 IF-01 总线注册适配 | 11.7 | **入选**（API 域同主题） |
| API-006 IF-04 门户嵌入 API | 12.0 | **入选**（API 域同主题） |
| API-007 OpenAPI 规范与版本策略 | 13.1 | **入选**（API 域末项，补足第 5 项） |
| CONN-016 OpenSearch | 11.6 | 连接器域 r40–r41 已大量演化，不与 API 混批 |
| DESIGN-003/005 | 11.6 | 设计器域（M13），依赖 Dataset/工单上下文，远期项 |

## 演化北极星自检

1. **用户感知**：平台对外集成能力从「仅 health/me 占位」扩展为查询服务、总线注册、报表文档、门户嵌入四类 IF 契约可调用（骨架级），开放 API 文档具备版本策略声明。
2. **补缺 or 创造**：补缺（completeness 5%、状态=未实现）；符合 `goal.md` G5「查询服务治理与总线对接」与 In Scope「Web 门户与开放 API」。
3. **不做代价**：集成 API 持续缺失，政企总线对接与查询服务发布无法形成平台化闭环，对标差异化能力落空。
4. **能否批处理更小项**：已批处理为 F13-API 域 5 项 L1 kickoff。
5. **共几项/文件模块**：5 项；集成 API 路由簇 + tests，估 ≤20 文件、≤3 模块。

---

### 子项 1：API-003 IF-02 查询服务 API
- **选题理由**：全表加权总分最低（11.3），完整度/可靠性/测试/性能全部 0–5%，未实现。
- **选题时 PRD 加权总分**：11.3/100（价 47 / 完 5 / 靠 0 / 交 N/A / 架 8 / 测 0 / 性 0 / 安 10）
- **主攻薄弱维**：完整度（5%）；连带可靠性/测试/性能（0%）
- **用户感知**：外部系统可通过标准 REST 调用已发布查询服务（骨架契约）。
- **类型**：补缺
- **验收标准（L1 kickoff）**：查询服务 API 路由 + request/response schema + 错误域 + auth 守卫 + smoke 测试；核心维脱离 0–5% 骨架档（详见 `prd/F13-API.md`，P1 设计时加载）。

### 子项 2：API-005 IF-03 报表文档 API
- **选题理由**：API 域同主题薄弱项（11.6），未实现。
- **选题时 PRD 加权总分**：11.6/100（价 46 / 完 5 / 靠 0 / 交 N/A / 架 10 / 测 0 / 性 0 / 安 12）
- **主攻薄弱维**：完整度（5%）；连带可靠性/测试/性能（0%）
- **用户感知**：外部系统可获取报表文档元数据与导出入口（骨架契约）。
- **类型**：补缺
- **验收标准（L1 kickoff）**：报表文档 API 路由 + schema + 错误域 + smoke 测试；核心维脱离 0–5% 骨架档。

### 子项 3：API-004 IF-01 总线注册适配
- **选题理由**：API 域同主题薄弱项（11.7），未实现；与 API-003 查询服务形成「发布→总线注册」链路。
- **选题时 PRD 加权总分**：11.7/100（价 48 / 完 5 / 靠 0 / 交 N/A / 架 9 / 测 0 / 性 0 / 安 11）
- **主攻薄弱维**：完整度（5%）；连带可靠性/测试/性能（0%）
- **用户感知**：查询服务发布后可向数据交换总线注册（骨架适配层）。
- **类型**：补缺
- **验收标准（L1 kickoff）**：总线注册适配 API + 注册 payload schema + 错误域 + mock/smoke 测试；核心维脱离 0–5% 骨架档。

### 子项 4：API-006 IF-04 门户嵌入 API
- **选题理由**：API 域同主题薄弱项（12.0），补足嵌入场景契约。
- **选题时 PRD 加权总分**：12.0/100（价 47 / 完 5 / 靠 0 / 交 N/A / 架 11 / 测 0 / 性 0 / 安 13）
- **主攻薄弱维**：完整度（5%）；连带可靠性/测试/性能（0%）
- **用户感知**：门户/第三方页面可获取图表/仪表板嵌入令牌与配置（骨架契约）。
- **类型**：补缺
- **验收标准（L1 kickoff）**：门户嵌入 API 路由 + embed token/schema + origin 守卫 smoke + 测试；核心维脱离 0–5% 骨架档。

### 子项 5：API-007 OpenAPI 规范与版本策略
- **选题理由**：API 域末项（13.1），补足第 5 项；为前述 IF 契约提供 OpenAPI 版本策略声明。
- **选题时 PRD 加权总分**：13.1/100（价 56 / 完 5 / 靠 0 / 交 N/A / 架 12 / 测 0 / 性 0 / 安 8）
- **主攻薄弱维**：完整度（5%）；连带可靠性/测试（0%）
- **用户感知**：`/docs` OpenAPI 文档具备版本策略与 IF 路由分组，集成方可见稳定契约边界。
- **类型**：补缺
- **验收标准（L1 kickoff）**：OpenAPI 版本策略配置 + IF 路由 tag 分组 + schema 稳定性 smoke + 测试；核心维脱离 0–5% 骨架档。
