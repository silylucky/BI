# M-FE-2 P1 最小出数闭环实现计划

> **执行模式：** subagent-driven-development (option 1)
> **范围框定：** `fe/`（Schema 浏览、Widget SQL 配置、Dashboard 网格拖拽）+ `tests/test_data_p1_smoke_l2.py` + `docs/automate/prd/F03-DS.md` · `F07-DASH.md` · `F16-DATA.md`
> **子项：** DS-004, VIZ-002, DASH-002, DATA-005
> **项目技能：** `.agents/skills/`（P3 按 Files 按需 Read；plan 预指定 **Skills:**）
> **项目规则：** `.cursor/rules/`（alwaysApply 自动注入；触及 `fe/**` 匹配 `fe-ui.mdc`）

**Goal:** 浏览器端打通 Schema 浏览 → Dashboard widget SQL 配置 → 网格拖拽布局 → view 模式图表出数，并以 pytest DATA-SMOKE L2 + vitest smoke 可审计验收。

**Architecture:** DS-004 在数据源详情页用 TanStack Query 懒加载三级 metadata API；VIZ-002 在 edit 模式 widget 内嵌 `WidgetSqlPanel` 写回 `chartConfig`；DASH-002 用 `react-grid-layout` 12 列拖拽，`gridLayoutAdapter` 仅写回 `order/colSpan/rowSpan`（不改后端 schema）；DATA-005 用 TestClient 编排建源→execute→dashboard layout 全链路并回写 PRD。

**Tech Stack:** React 19 · TanStack Query · react-grid-layout · Vitest · pytest · FastAPI TestClient

## Global Constraints

- 真理源：`docs/superpowers/evolution/2026-07-06-round-target-m-fe-2.md` > `docs/superpowers/specs/2026-07-06-m-fe-2-p1-smoke-design.md`
- `base_branch`: `dev-auto`；禁止修改 `docs/automate/goal.md`；P5 前不改 `plan.md` 结构
- 非目标：角色 Admin UI、全局筛选器 FE、后端 `layout_json` 增字段、Playwright E2E、M-FE-3 项
- 图表直连 `dataSourceId` + SQL，不经 Dataset
- 文件预算：**18** 主文件（design §3）；`ui/collapsible.tsx` 与 `App.tsx` sonner 计入 UI 基元补齐，总计 ≤20
- 后端验证：`cd backend && python3 -m ruff check . && python3 -m pytest tests/test_data_p1_smoke_l2.py -q`
- 前端验证：`cd fe && pnpm install && pnpm run check:design && pnpm test && pnpm build`

---

### Task 1: DS-004 Schema 元数据浏览（queryKeys + SchemaBrowser + 详情页集成）

**Files:**
- Create: `fe/src/components/ui/collapsible.tsx`
- Create: `fe/src/components/datasources/schemaBrowserUtils.ts`
- Create: `fe/src/components/datasources/SchemaBrowser.tsx`
- Create: `fe/src/pages/admin/datasources/datasource-detail.smoke.test.tsx`
- Modify: `fe/src/lib/queryKeys.ts`
- Modify: `fe/src/pages/admin/datasources/DatasourceDetailPage.tsx`
- Modify: `fe/package.json`（增 `sonner`）
- Modify: `fe/src/App.tsx`（挂载 `<Toaster />`）

**Skills:**
- Read `.agents/skills/b-design-system-tailadmin-radix/SKILL.md`
- Read `.agents/skills/test-driven-development/SKILL.md`

**UI Acceptance:**
- 复用 TailAdmin Card + Collapsible 树；`max-w-3xl` 与连接信息同宽
- desktop/mobile 树行不横向溢出（`truncate` + `title`）
- loading Skeleton、empty 中文、error 红框 + 重试、403 无权文案
- 复制/生成 SELECT 有 toast 反馈；`cd fe && pnpm run check:design` 无硬编码 hex

**Interfaces:**
- Consumes: `GET /api/v1/datasources/{id}/schemas|tables?schema=|columns?schema=&table=`
- Produces: `queryKeys.datasources.schemas/tables/columns`；`mapMetadataError(code)`；`qualifiedTableName(schema, table)`；`SchemaBrowser({ dataSourceId })`

- [ ] **Step 1: 安装 sonner 并在 App 挂载 Toaster**

```bash
cd fe && pnpm add sonner
```

`fe/src/App.tsx` 追加：

```tsx
import { Toaster } from "sonner";
// ... inside return, after </BrowserRouter> closing or inside AuthProvider:
<Toaster position="top-center" richColors />
```

- [ ] **Step 2: 扩展 queryKeys**

`fe/src/lib/queryKeys.ts` 在 `datasources` 对象内追加：

```ts
    schemas: (id: string) => ["datasources", id, "schemas"] as const,
    tables: (id: string, schema: string) => ["datasources", id, "tables", schema] as const,
    columns: (id: string, schema: string, table: string) =>
      ["datasources", id, "columns", schema, table] as const,
```

- [ ] **Step 3: 创建 `schemaBrowserUtils.ts`**

```ts
import { mapApiError } from "@/lib/apiError";

const METADATA_MESSAGES: Record<string, string> = {
  METADATA_CONNECTION_FAILED: "无法连接数据源，请先在上方测试连通性",
  METADATA_TIMEOUT: "元数据查询超时，请稍后重试",
  METADATA_NOT_SUPPORTED: "该连接器不支持元数据浏览",
  METADATA_INVALID_REQUEST: "请求参数无效",
  RESOURCE_FORBIDDEN: "无权访问该数据源",
};

export function mapMetadataError(err: unknown): string {
  const code = (err as { code?: string })?.code;
  if (code && METADATA_MESSAGES[code]) return METADATA_MESSAGES[code];
  return mapApiError(err);
}

export function qualifiedTableName(schema: string, table: string): string {
  return `${schema}.${table}`;
}

export function buildSelectSql(schema: string, table: string): string {
  return `SELECT * FROM ${qualifiedTableName(schema, table)} LIMIT 100`;
}
```

- [ ] **Step 4: 创建 `ui/collapsible.tsx`（Radix 薄封装）**

```tsx
import * as CollapsiblePrimitive from "@radix-ui/react-collapsible";
import { cn } from "@/lib/utils";

const Collapsible = CollapsiblePrimitive.Root;
const CollapsibleTrigger = CollapsiblePrimitive.CollapsibleTrigger;
const CollapsibleContent = CollapsiblePrimitive.CollapsibleContent;

export { Collapsible, CollapsibleTrigger, CollapsibleContent };
```

- [ ] **Step 5: 写失败 smoke 测试**

`fe/src/pages/admin/datasources/datasource-detail.smoke.test.tsx`：

```tsx
import { QueryClient, QueryClientProvider } from "@tanstack/react-query";
import { cleanup, render, screen, waitFor } from "@testing-library/react";
import userEvent from "@testing-library/user-event";
import { MemoryRouter, Route, Routes } from "react-router";
import { afterEach, beforeEach, describe, expect, it, vi } from "vitest";

const mockApiFetch = vi.fn();
vi.mock("@/lib/api", () => ({ apiFetch: (...args: unknown[]) => mockApiFetch(...args) }));
vi.mock("@/context/auth-context", () => ({
  useAuth: () => ({ user: { id: "1", username: "admin", roles: ["admin"] }, isLoading: false, isAuthenticated: true, logout: vi.fn(), refresh: vi.fn(async () => {}) }),
  AuthProvider: ({ children }: { children: React.ReactNode }) => children,
}));

import { DatasourceDetailPage } from "./DatasourceDetailPage";

function renderDetail(id = "ds-1") {
  const qc = new QueryClient({ defaultOptions: { queries: { retry: false } } });
  return render(
    <QueryClientProvider client={qc}>
      <MemoryRouter initialEntries={[`/admin/datasources/${id}`]}>
        <Routes>
          <Route path="/admin/datasources/:id" element={<DatasourceDetailPage />} />
        </Routes>
      </MemoryRouter>
    </QueryClientProvider>,
  );
}

describe("datasource detail schema browser", () => {
  beforeEach(() => {
    mockApiFetch.mockReset();
    Object.assign(navigator, {
      clipboard: { writeText: vi.fn().mockResolvedValue(undefined) },
    });
  });
  afterEach(() => cleanup());

  it("T-DS-004-01: renders 元数据浏览 card and expands schema", async () => {
    const user = userEvent.setup();
    mockApiFetch
      .mockResolvedValueOnce({ id: "ds-1", name: "分析库", code: "analytics", type: "postgresql", host: "h", port: 5432, database: "d", username: "u" })
      .mockResolvedValueOnce({ items: [{ name: "public" }] })
      .mockResolvedValueOnce({ items: [{ name: "orders", type: "table" }] });
    renderDetail();
    expect(await screen.findByText("元数据浏览")).toBeInTheDocument();
    await user.click(screen.getByRole("button", { name: /public/i }));
    await waitFor(() => expect(mockApiFetch).toHaveBeenCalledWith("/api/v1/datasources/ds-1/tables?schema=public"));
    expect(await screen.findByText("orders")).toBeInTheDocument();
  });

  it("T-DS-004-02: METADATA_CONNECTION_FAILED shows Chinese error", async () => {
    mockApiFetch
      .mockResolvedValueOnce({ id: "ds-1", name: "X", code: "x", type: "postgresql", host: "h", port: 1, database: "d", username: "u" })
      .mockRejectedValueOnce(Object.assign(new Error("fail"), { code: "METADATA_CONNECTION_FAILED" }));
    renderDetail();
    expect(await screen.findByText(/无法连接数据源/)).toBeInTheDocument();
  });
});
```

Run: `cd fe && pnpm vitest run src/pages/admin/datasources/datasource-detail.smoke.test.tsx`
Expected: FAIL（SchemaBrowser 未实现）

- [ ] **Step 6: 实现 `SchemaBrowser.tsx`**

核心结构（完整文件 <200 行；子组件 `SchemaRow`/`TableRow` 可内联）：

```tsx
import { useState } from "react";
import { useQuery } from "@tanstack/react-query";
import { ChevronRight, Copy, MoreHorizontal } from "lucide-react";
import { toast } from "sonner";
import { apiFetch } from "@/lib/api";
import { queryKeys } from "@/lib/queryKeys";
import { Badge } from "@/components/ui/badge";
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Collapsible, CollapsibleContent, CollapsibleTrigger } from "@/components/ui/collapsible";
import {
  DropdownMenu, DropdownMenuContent, DropdownMenuItem, DropdownMenuTrigger,
} from "@/components/ui/dropdown-menu";
import { Skeleton } from "@/components/ui/skeleton";
import { buildSelectSql, mapMetadataError, qualifiedTableName } from "./schemaBrowserUtils";

type SchemaItem = { name: string };
type TableItem = { name: string; type: string };
type ColumnItem = { name: string; dataType: string; nullable: boolean };

async function copyText(text: string, label: string) {
  await navigator.clipboard.writeText(text);
  toast.success(`已复制${label}`);
}

export function SchemaBrowser({ dataSourceId }: { dataSourceId: string }) {
  const [openSchemas, setOpenSchemas] = useState<Record<string, boolean>>({});
  const [openTables, setOpenTables] = useState<Record<string, boolean>>({});

  const schemasQuery = useQuery({
    queryKey: queryKeys.datasources.schemas(dataSourceId),
    queryFn: () => apiFetch<{ items: SchemaItem[] }>(`/api/v1/datasources/${dataSourceId}/schemas`),
    enabled: Boolean(dataSourceId),
  });

  if (schemasQuery.isLoading) {
    return <Card className="max-w-3xl"><CardHeader><CardTitle>元数据浏览</CardTitle></CardHeader><CardContent><Skeleton className="h-8 w-full" /></CardContent></Card>;
  }
  if (schemasQuery.isError) {
    const msg = mapMetadataError(schemasQuery.error);
    return (
      <Card className="max-w-3xl">
        <CardHeader><CardTitle>元数据浏览</CardTitle></CardHeader>
        <CardContent>
          <div className="rounded-xl border border-error-500 bg-error-50 p-4 text-theme-sm text-error-700 dark:border-error-500/30 dark:bg-error-500/15 dark:text-error-400">
            <p>{msg}</p>
            <Button className="mt-3" variant="outline" size="sm" onClick={() => void schemasQuery.refetch()}>重试</Button>
          </div>
        </CardContent>
      </Card>
    );
  }

  const schemas = schemasQuery.data?.items ?? [];

  return (
    <Card className="max-w-3xl">
      <CardHeader><CardTitle>元数据浏览</CardTitle></CardHeader>
      <CardContent className="space-y-1">
        {schemas.length === 0 ? (
          <p className="text-theme-sm text-gray-500">暂无 schema</p>
        ) : (
          schemas.map((schema) => (
            <SchemaSection
              key={schema.name}
              dataSourceId={dataSourceId}
              schema={schema.name}
              open={Boolean(openSchemas[schema.name])}
              onOpenChange={(o) => setOpenSchemas((s) => ({ ...s, [schema.name]: o }))}
              openTables={openTables}
              setOpenTables={setOpenTables}
            />
          ))
        )}
      </CardContent>
    </Card>
  );
}
```

`SchemaSection` 内：`tables` query `enabled: open`；`columns` query `enabled: openTables[schema.tableKey]`；表行 `DropdownMenu` 含「复制表名」「生成 SELECT」调用 `copyText`。

- [ ] **Step 7: 集成 `DatasourceDetailPage.tsx`**

在连接信息 `Card` 之后、`testError` 区块之前插入：

```tsx
import { SchemaBrowser } from "@/components/datasources/SchemaBrowser";
// ...
      <div className="mt-6">
        <SchemaBrowser dataSourceId={id} />
      </div>
```

- [ ] **Step 8: 运行测试**

Run: `cd fe && pnpm vitest run src/pages/admin/datasources/datasource-detail.smoke.test.tsx -v`
Expected: PASS

- [ ] **Step 9: Commit**

```bash
git add fe/src/components/ui/collapsible.tsx fe/src/components/datasources/ fe/src/lib/queryKeys.ts fe/src/pages/admin/datasources/ fe/package.json fe/pnpm-lock.yaml fe/src/App.tsx
git commit -m "feat(fe): DS-004 schema metadata browser on datasource detail"
```

---

### Task 2: VIZ-002 Widget SQL 配置与出数绑定

**Files:**
- Create: `fe/src/components/dashboard/WidgetSqlPanel.tsx`
- Modify: `fe/src/components/dashboard/layoutUtils.ts`
- Modify: `fe/src/components/dashboard/DashboardWidget.tsx`
- Modify: `fe/src/components/README.md`

**Skills:**
- Read `.agents/skills/b-design-system-tailadmin-radix/SKILL.md`

**UI Acceptance:**
- edit 模式 SQL 面板默认展开：Select 数据源 + monospace Textarea + 图表类型 Badge
- 未选数据源 inline「请选择数据源」（非 toast）
- view 模式不渲染 SqlPanel，仅 ChartRenderer 三态
- desktop/mobile 面板不挤压标题栏；错误态复用 ChartPanel

**Interfaces:**
- Consumes: `queryKeys.datasources.list()`；`ChartViewConfig`；`mapChartQueryError` from `useChartExecute.ts`
- Produces: `WidgetSqlPanel({ widget, onChange })`；`defaultChartConfig` 返回 `dataSourceId: ""`

- [ ] **Step 1: 修改 `layoutUtils.ts` 移除占位 dataSourceId**

```ts
export function defaultChartConfig(type: ChartTypeL1): ChartViewConfig {
  const base = {
    dataSourceId: "",
    mode: "sql" as const,
  };
  if (type === "table") {
    return { chartType: "table", ...base, sql: "SELECT 1 AS id", dimensions: [], metrics: [] };
  }
  return {
    chartType: type,
    ...base,
    sql: "SELECT 1 AS x, 1 AS y",
    dimensions: [{ field: "x" }],
    metrics: [{ field: "y" }],
  };
}
```

- [ ] **Step 2: 创建 `WidgetSqlPanel.tsx`**

```tsx
import { useQuery } from "@tanstack/react-query";
import { apiFetch } from "@/lib/api";
import { queryKeys } from "@/lib/queryKeys";
import type { ChartViewConfig } from "@/lib/chartViewConfig";
import { Badge } from "@/components/ui/badge";
import { Label } from "@/components/ui/label";
import {
  Select, SelectContent, SelectItem, SelectTrigger, SelectValue,
} from "@/components/ui/select";
import { cn } from "@/lib/utils";
import type { LayoutWidget } from "./layoutUtils";

type DataSourceListItem = { id: string; name: string; code: string };

type WidgetSqlPanelProps = {
  widget: LayoutWidget;
  onChange: (chartConfig: ChartViewConfig) => void;
};

export function WidgetSqlPanel({ widget, onChange }: WidgetSqlPanelProps) {
  const { data } = useQuery({
    queryKey: queryKeys.datasources.list(),
    queryFn: () => apiFetch<{ items: DataSourceListItem[] }>("/api/v1/datasources"),
  });
  const cfg = widget.chartConfig;
  const dsMissing = !cfg.dataSourceId;

  return (
    <div className="space-y-3 border-b border-gray-100 px-4 py-3 dark:border-gray-800">
      <div className="flex flex-wrap items-center gap-2">
        <Badge variant="light">{cfg.chartType}</Badge>
        {dsMissing ? <span className="text-theme-xs text-error-500">请选择数据源</span> : null}
      </div>
      <div className="grid gap-2">
        <Label htmlFor={`ds-${widget.id}`}>数据源</Label>
        <Select
          value={cfg.dataSourceId || undefined}
          onValueChange={(dataSourceId) => onChange({ ...cfg, dataSourceId })}
        >
          <SelectTrigger id={`ds-${widget.id}`} className="h-9">
            <SelectValue placeholder="选择数据源" />
          </SelectTrigger>
          <SelectContent>
            {(data?.items ?? []).map((ds) => (
              <SelectItem key={ds.id} value={ds.id}>{ds.name} ({ds.code})</SelectItem>
            ))}
          </SelectContent>
        </Select>
      </div>
      <div className="grid gap-2">
        <Label htmlFor={`sql-${widget.id}`}>SQL</Label>
        <textarea
          id={`sql-${widget.id}`}
          value={cfg.sql ?? ""}
          onChange={(e) => onChange({ ...cfg, sql: e.target.value })}
          rows={4}
          className={cn(
            "w-full rounded-lg border border-gray-300 bg-transparent px-3 py-2 font-mono text-theme-sm text-gray-800 dark:border-gray-700 dark:text-white/90",
          )}
        />
      </div>
    </div>
  );
}
```

- [ ] **Step 3: 修改 `DashboardWidget.tsx`**

增 props `onChartConfigChange?: (id: string, config: ChartViewConfig) => void`；edit 模式在标题栏下渲染：

```tsx
import { WidgetSqlPanel } from "./WidgetSqlPanel";
// edit 模式 body 顶部：
{mode === "edit" && onChartConfigChange ? (
  <WidgetSqlPanel
    widget={widget}
    onChange={(chartConfig) => onChartConfigChange(widget.id, chartConfig)}
  />
) : null}
```

view 模式保持仅 `ChartRenderer`。

- [ ] **Step 4: 更新 `components/README.md`**

追加行：

```markdown
| SchemaBrowser | `datasources/SchemaBrowser.tsx` | 数据源详情三级 metadata 树 |
| WidgetSqlPanel | `dashboard/WidgetSqlPanel.tsx` | Dashboard edit widget SQL/数据源配置 |
```

- [ ] **Step 5: 验证**

Run: `cd fe && pnpm vitest run src/components/charts/charts.smoke.test.tsx -v`
Expected: PASS（ChartRenderer 未破坏）

Run: `cd fe && pnpm run check:design`
Expected: exit 0

- [ ] **Step 6: Commit**

```bash
git add fe/src/components/dashboard/WidgetSqlPanel.tsx fe/src/components/dashboard/DashboardWidget.tsx fe/src/components/dashboard/layoutUtils.ts fe/src/components/README.md
git commit -m "feat(fe): VIZ-002 widget SQL panel binds dataSourceId and sql"
```

---

### Task 3: DASH-002 react-grid-layout 拖拽与 edit/view 切换

**Files:**
- Create: `fe/src/components/dashboard/gridLayoutAdapter.ts`
- Modify: `fe/package.json`（`react-grid-layout`、`@types/react-grid-layout`）
- Modify: `fe/src/components/dashboard/DashboardGrid.tsx`
- Modify: `fe/src/components/dashboard/DashboardWidget.tsx`（drag handle）
- Modify: `fe/src/pages/admin/dashboard/DashboardEditPage.tsx`

**Skills:**
- Read `.agents/skills/b-design-system-tailadmin-radix/SKILL.md`

**UI Acceptance:**
- edit 模式左侧 `GripVertical` 拖拽手柄；view 模式无手柄、CSS grid 静态布局
- 顶栏 edit「预览」→ view 路由；view「编辑布局」→ edit 路由
- RGL 样式与 TailAdmin token 一致；mobile 单列不重叠

**Interfaces:**
- Consumes: `LayoutWidget[]`；`react-grid-layout` `Layout[]`
- Produces: `widgetsToGridLayout(widgets)`；`gridLayoutToWidgets(layout, widgets)`；`onLayoutChange` 写回 widgets

- [ ] **Step 1: 安装依赖**

```bash
cd fe && pnpm add react-grid-layout && pnpm add -D @types/react-grid-layout
```

`fe/package.json` 确认 dependencies 含 `"react-grid-layout": "^1.5.0"`（或安装时解析的最新 1.x）。

- [ ] **Step 2: 创建 `gridLayoutAdapter.ts`**

```ts
import type { Layout } from "react-grid-layout";
import type { LayoutWidget } from "./layoutUtils";
import { sortWidgets } from "./layoutUtils";

const COL_SPANS: LayoutWidget["colSpan"][] = [4, 6, 8, 12];
export const GRID_ROW_HEIGHT = 80;
const MIN_CHART_ROWS = 2;

function snapColSpan(w: number): LayoutWidget["colSpan"] {
  let best: LayoutWidget["colSpan"] = 4;
  let min = Number.POSITIVE_INFINITY;
  for (const span of COL_SPANS) {
    const d = Math.abs(w - span);
    if (d < min) {
      min = d;
      best = span;
    }
  }
  return best;
}

export function widgetsToGridLayout(widgets: LayoutWidget[]): Layout[] {
  const sorted = sortWidgets(widgets);
  let x = 0;
  let y = 0;
  let rowMaxH = 0;
  const out: Layout[] = [];
  for (const w of sorted) {
    const h = Math.max(MIN_CHART_ROWS, w.rowSpan);
    if (x + w.colSpan > 12) {
      x = 0;
      y += rowMaxH;
      rowMaxH = 0;
    }
    out.push({ i: w.id, x, y, w: w.colSpan, h, minW: 4, maxW: 12, minH: 1, maxH: 8 });
    x += w.colSpan;
    rowMaxH = Math.max(rowMaxH, h);
    if (x >= 12) {
      x = 0;
      y += rowMaxH;
      rowMaxH = 0;
    }
  }
  return out;
}

export function gridLayoutToWidgets(layout: Layout[], widgets: LayoutWidget[]): LayoutWidget[] {
  const byId = new Map(widgets.map((w) => [w.id, w]));
  const sorted = [...layout].sort((a, b) => a.y - b.y || a.x - b.x);
  return sorted.map((item, order) => {
    const base = byId.get(item.i);
    if (!base) throw new Error(`Unknown widget ${item.i}`);
    return {
      ...base,
      colSpan: snapColSpan(item.w),
      rowSpan: Math.min(8, Math.max(1, item.h)),
      order,
    };
  });
}
```

- [ ] **Step 3: 修改 `DashboardGrid.tsx`**

- `mode === "view"`：保持现有 CSS grid
- `mode === "edit"`：使用 `ResponsiveGridLayout`（`cols={{ xl: 12, lg: 12, md: 12, sm: 6, xs: 4 }}`，`rowHeight={GRID_ROW_HEIGHT}`，`isDraggable`/`isResizable` true，`draggableHandle=".dashboard-drag-handle"`）
- 顶部 `import "react-grid-layout/css/styles.css"`
- props 增 `onLayoutChange?: (widgets: LayoutWidget[]) => void`

- [ ] **Step 4: `DashboardWidget.tsx` 增拖拽手柄**

edit 模式标题栏最左侧：

```tsx
import { GripVertical } from "lucide-react";
// inside edit title row:
<span className="dashboard-drag-handle cursor-grab text-gray-400" aria-label="拖拽组件">
  <GripVertical className="size-4" />
</span>
```

- [ ] **Step 5: 修改 `DashboardEditPage.tsx`**

顶栏 actions 追加互链：

```tsx
{mode === "edit" ? (
  <Button asChild variant="outline">
    <Link to={`/admin/dashboards/${id}`}>预览</Link>
  </Button>
) : (
  <Button asChild variant="outline">
    <Link to={`/admin/dashboards/${id}/edit`}>编辑布局</Link>
  </Button>
)}
```

`DashboardGrid` 传入：

```tsx
onLayoutChange={(next) => setWidgets(sortWidgets(next))}
onChartConfigChange={(wid, chartConfig) =>
  setWidgets((prev) => prev.map((w) => (w.id === wid ? { ...w, chartConfig } : w)))
}
```

保存前校验：若任一 widget `!chartConfig.dataSourceId`，`setError("请为所有组件选择数据源")` 并 return。

- [ ] **Step 6: 验证 adapter 逻辑**

在 `dashboard.smoke.test.tsx` 追加：

```tsx
import { gridLayoutToWidgets, widgetsToGridLayout } from "@/components/dashboard/gridLayoutAdapter";
// ...
it("T-DASH-002-01: gridLayoutAdapter round-trip order", () => {
  const layout = widgetsToGridLayout(sampleWidgets);
  const next = gridLayoutToWidgets(layout, sampleWidgets);
  expect(next.map((w) => w.id)).toEqual(["w1", "w2", "w3"]);
});
```

Run: `cd fe && pnpm vitest run src/pages/admin/dashboard/dashboard.smoke.test.tsx -v`
Expected: PASS

- [ ] **Step 7: Commit**

```bash
git add fe/package.json fe/pnpm-lock.yaml fe/src/components/dashboard/ fe/src/pages/admin/dashboard/DashboardEditPage.tsx
git commit -m "feat(fe): DASH-002 react-grid-layout drag and edit/view switch"
```

---

### Task 4: Vitest smoke 扩展（VIZ-002 + DASH-002 保存 payload）

**Files:**
- Modify: `fe/src/pages/admin/dashboard/dashboard.smoke.test.tsx`

**Skills:**
- Read `.agents/skills/test-driven-development/SKILL.md`

**UI skill:** none（纯测试 Task）

- [ ] **Step 1: 写失败测试 — SQL 配置写入保存 payload**

```tsx
it("T-VIZ-002-01: save layout includes edited dataSourceId and sql", async () => {
  const user = userEvent.setup();
  const dsId = "00000000-0000-4000-8000-000000000010";
  let putBody: { layoutJson?: { widgets?: { chartConfig?: { dataSourceId?: string; sql?: string } }[] } } | undefined;
  mockApiFetch
    .mockResolvedValueOnce({
      id: "d1", name: "编辑",
      layoutJson: { version: 1, widgets: [{ ...sampleWidgets[0], chartConfig: { ...defaultChartConfig("table"), dataSourceId: "" } }], globalFilters: [] },
    })
    .mockResolvedValueOnce({ items: [{ id: dsId, name: "分析库", code: "a" }] })
    .mockImplementationOnce(async (_path, init) => {
      putBody = JSON.parse((init as RequestInit).body as string);
      return {};
    });
  render(/* DashboardEditPage mode=edit 同现有用例 */);
  await screen.findByLabelText("数据源");
  await user.click(screen.getByRole("combobox"));
  await user.click(await screen.findByText(/分析库/));
  const sql = screen.getByLabelText("SQL");
  await user.clear(sql);
  await user.type(sql, "SELECT 2 AS id");
  await user.click(screen.getByRole("button", { name: "保存布局" }));
  expect(putBody?.layoutJson?.widgets?.[0]?.chartConfig?.dataSourceId).toBe(dsId);
  expect(putBody?.layoutJson?.widgets?.[0]?.chartConfig?.sql).toBe("SELECT 2 AS id");
});
```

- [ ] **Step 2: view 模式使用真实 ChartRenderer（取消 mock）的独立用例**

新建用例文件内 `vi.unmock("@/components/charts/ChartRenderer")` 或在 describe 块局部 mock `apiFetch` 返回 rows，断言 `findByText` 表头（参考 `charts.smoke.test.tsx`）。

- [ ] **Step 3: 运行全量 fe 测试**

Run: `cd fe && pnpm test`
Expected: 全部 PASS

- [ ] **Step 4: Commit**

```bash
git add fe/src/pages/admin/dashboard/dashboard.smoke.test.tsx
git commit -m "test(fe): VIZ-002/DASH-002 dashboard smoke for SQL save and grid adapter"
```

---

### Task 5: DATA-005 DATA-SMOKE L2 pytest 与 PRD 回写

**Files:**
- Create: `tests/test_data_p1_smoke_l2.py`
- Modify: `docs/automate/prd/F16-DATA.md`
- Modify: `docs/automate/prd/F07-DASH.md`
- Modify: `docs/automate/prd/F03-DS.md`

**Skills:**
- Read `.agents/skills/fastapi/SKILL.md`
- Read `.agents/skills/test-driven-development/SKILL.md`

**UI skill:** none

**Interfaces:**
- Consumes: `client` + `auth_headers` fixtures；`@patch pool_manager.pooled_connection` 模式（`tests/test_query_l1_r26.py`）
- Produces: `test_p1_smoke_orchestrator` 串联 T-L2-01~05；PRD `[x]` 勾选与代码锚点

- [ ] **Step 1: 创建 `tests/test_data_p1_smoke_l2.py`**

```python
"""DATA-SMOKE L2 — dataSourceId + SQL → dashboard layout → query execute."""
from __future__ import annotations

import os
import time
import uuid
from contextlib import contextmanager
from unittest.mock import MagicMock, patch

import pytest
from fastapi.testclient import TestClient
from sqlalchemy import text

_DASH_SQLITE_URL = "sqlite+pysqlite:///file:data_p1_l2?mode=memory&cache=shared&uri=true"


@pytest.fixture(scope="module", autouse=True)
def l2_sqlite_env():
    previous = os.environ.get("DATABASE_URL")
    os.environ["DATABASE_URL"] = _DASH_SQLITE_URL
    from app.core.config import get_settings
    from app.auth.models import get_meta_engine as auth_get_meta_engine
    from app.datasources.models import Base, get_meta_engine
    from app.dashboard.models import Base as DashBase

    get_settings.cache_clear()
    get_meta_engine.cache_clear()
    auth_get_meta_engine.cache_clear()
    engine = get_meta_engine()
    Base.metadata.create_all(engine)
    DashBase.metadata.create_all(engine)
    with engine.begin() as conn:
        conn.execute(text("DELETE FROM dashboards"))
        conn.execute(text("DELETE FROM data_sources"))
    yield
    if previous is None:
        os.environ.pop("DATABASE_URL", None)
    else:
        os.environ["DATABASE_URL"] = previous
    get_settings.cache_clear()
    get_meta_engine.cache_clear()
    auth_get_meta_engine.cache_clear()


def _mock_pool_cursor(columns, rows):
    cur = MagicMock()
    cur.description = [(c,) for c in columns]
    cur.fetchmany.return_value = rows
    conn = MagicMock()
    conn.cursor.return_value = cur

    @contextmanager
    def _cm(*_a, **_k):
        yield conn

    return _cm


def _run_l2_chain(client: TestClient, auth_headers: dict[str, str]) -> str:
    """Returns dashboard id after create + layout + execute assertions."""
    from app.datasources.schemas import DataSourceCreate
    from app.datasources.models import get_meta_session
    from app.datasources.service import create_data_source

    session = get_meta_session()
    try:
        ds = create_data_source(
            session,
            DataSourceCreate(
                name="L2 Chain DS", code=f"l2c-{uuid.uuid4().hex[:8]}", type="postgresql",
                host="h", port=5432, database="d", username="u", password="p",
            ),
        )
        session.commit()
        ds_id = str(ds.id)
    finally:
        session.close()

    with patch("app.query.executor.pool_manager.pooled_connection") as mock_pool:
        mock_pool.side_effect = _mock_pool_cursor(["id"], [(1,)])
        exec_resp = client.post(
            "/api/v1/query/execute",
            json={"dataSourceId": ds_id, "mode": "sql", "sql": "SELECT 1 AS id", "limit": 10, "rls": {"enabled": False}},
            headers=auth_headers,
        )
        assert exec_resp.status_code == 200
        assert len(exec_resp.json()["rows"]) >= 1

        create = client.post(
            "/api/v1/dashboards",
            json={"name": "L2 Chain", "slug": f"l2c-{uuid.uuid4().hex[:6]}"},
            headers=auth_headers,
        )
        assert create.status_code == 201
        dash_id = create.json()["id"]
        layout = {
            "version": 1,
            "widgets": [{
                "id": str(uuid.uuid4()), "type": "chart", "title": "表",
                "colSpan": 6, "rowSpan": 2, "order": 0,
                "chartConfig": {"chartType": "table", "dataSourceId": ds_id, "mode": "sql", "sql": "SELECT 1 AS id"},
            }],
            "globalFilters": [],
        }
        put = client.put(f"/api/v1/dashboards/{dash_id}/layout", json={"layoutJson": layout}, headers=auth_headers)
        assert put.status_code == 200
        got = client.get(f"/api/v1/dashboards/{dash_id}", headers=auth_headers)
        assert got.json()["layoutJson"]["widgets"][0]["chartConfig"]["dataSourceId"] == ds_id
    return dash_id


@patch("app.query.executor.pool_manager.pooled_connection")
def test_l2_query_execute_returns_rows(mock_pool, client, auth_headers):
    """T-L2-02: POST execute sql → columns + rows >= 1."""
    mock_pool.side_effect = _mock_pool_cursor(["id"], [(1,)])
    dash_id = _run_l2_chain(client, auth_headers)
    assert dash_id


def test_p1_smoke_orchestrator(client, auth_headers):
    """T-L2-06: 编排全链路，耗时 <5s。"""
    started = time.perf_counter()
    _run_l2_chain(client, auth_headers)
    assert time.perf_counter() - started < 5.0
```

`test_l2_dashboard_layout_persists_chart_config` 可合并进 `_run_l2_chain`；若保留独立用例，从 helper 提取 `ds_id` 创建段复用。

- [ ] **Step 2: 运行 pytest**

Run: `cd backend && python3 -m pytest tests/test_data_p1_smoke_l2.py -q`
Expected: exit 0

- [ ] **Step 3: PRD 回写**

`docs/automate/prd/F03-DS.md` DS-004 验收追加：

```markdown
  - [x] Admin UI schema 三级浏览（数据源详情页）
```

代码锚点追加：`fe/src/components/datasources/SchemaBrowser.tsx`

`docs/automate/prd/F07-DASH.md` DASH-002：

```markdown
  - [x] 网格布局可拖拽（react-grid-layout + edit/view 切换）
```

锚点追加：`fe/src/components/dashboard/gridLayoutAdapter.ts` · `fe/src/components/dashboard/DashboardGrid.tsx`

`docs/automate/prd/F16-DATA.md` DATA-005 验收追加：

```markdown
  - [x] DATA-SMOKE L2：dataSourceId + SQL 出数（`tests/test_data_p1_smoke_l2.py`）
```

描述段 L2 从「待 M3/M4」改为「M-FE-2 已实现」。

- [ ] **Step 4: 全量验证**

Run: `cd backend && python3 -m ruff check . && python3 -m pytest -q`
Expected: exit 0

Run: `cd fe && pnpm run check:design && pnpm test && pnpm build`
Expected: exit 0

- [ ] **Step 5: Commit**

```bash
git add tests/test_data_p1_smoke_l2.py docs/automate/prd/F03-DS.md docs/automate/prd/F07-DASH.md docs/automate/prd/F16-DATA.md
git commit -m "test: DATA-005 P1-SMOKE L2 orchestrator and PRD checkboxes"
```

---

## Self-Review

| 检查项 | 结果 |
|--------|------|
| DS-004 / VIZ-002 / DASH-002 / DATA-005 各有 Task | Task 1–5 覆盖 |
| 无 TBD/TODO/适当处理 | 通过 |
| 每 Task 含验证命令 | 通过 |
| 前端 UI Task 含 Skills + UI Acceptance | Task 1–3 |
| 文件预算 ≤20 | 18 + collapsible + App sonner = 20 |
| 未超出 design 非目标 | 通过 |
