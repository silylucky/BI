# 实现计划（P2）

由 **evolution-planner** 产出（叠加 Superpowers `writing-plans`），文件名：

```
YYYY-MM-DD-<name>.md
```

要求（evolution-plan 硬约束）：

- 无占位符；每步含文件路径与验证命令
- 与 [`docs/automate/plan.md`](../../automate/plan.md) **严格区分**：plan.md = 里程碑顺序；本目录 = 单轮可执行合同

输入：当轮 [`../specs/`](../specs/) `*-design.md` + `round-target.md`。

P3 **evolution-implementer** 全文消费本目录计划实施。

本目录无文件时表示当前无活跃实现计划。
