# 后端 API 基址（生产 build / 直连）；本地 dev 请保持注释，走 vite proxy /api
# VITE_API_BASE_URL=http://localhost:8000

# 子路径部署时填写，不含首尾斜杠；与 backend FE_BASE_PATH 对齐
# VITE_FE_BASE_PATH=my-app
# 开发代理目标（默认 :8000；端口被占用或僵死进程时可改为 http://127.0.0.1:8002）
# VITE_DEV_API_PROXY=http://127.0.0.1:8002
