# BI 问数系统：插件功能开发对话记录

> 记录范围：从“开始实施”外部插件功能起，到插件管理、调用说明、GitHub 上传及界面调整完成为止。  
> 已按要求排除报错排查、异常修复与调试过程。

---

## 1. 开始实施

**用户：**

> 开始实施

**助手：**

开始为 BI 问数系统实现外部 JavaScript 插件能力，目标为：

- 通过界面添加本地路径或 ZIP 插件包；
- 自动读取插件目录中的 `plugin.json`；
- 识别并注册插件工具；
- 将插件、工具、配置与调用审计保存到 SQLite；
- 将启用的工具提供给 Agent 的 Function Calling；
- 对删除、发布、上传等高风险工具增加聊天内确认；
- 提供插件管理界面，支持查看、启停、配置、预检与卸载。

---

## 2. 插件兼容方案

**助手：**

针对 VitalSpan 插件，系统实现了 Node.js 运行时适配器，支持三种导出协议：

1. **VitalSpan / DeepTalk Tool**
   - 发现方式：`tool.info()`；
   - 执行方式：`tool.run(context, call)`。

2. **ExecTool**
   - 执行方式：`run(params, context)`；
   - 默认仅识别和展示，可按需启用。

3. **通用标准工具**
   - 元数据：`manifest`；
   - 执行函数：`execute(args, context)`。

后端通过 Node 子进程加载 JavaScript 模块，Python 侧不直接执行插件源码。识别出的工具会包装为现有的 `BasePlugin`，从而可以由 `PluginRegistry` 和 Agent 统一管理。

---

## 3. 插件管理能力

**助手：**

实现的插件管理能力包括：

- 从本地目录安装插件；
- 上传 ZIP 安装插件；
- 读取 `plugin.json` 中的 `components.tools` 和 `components.execTools`；
- 查看插件名称、版本、描述、健康状态和工具列表；
- 启用或停用插件；
- 编辑插件运行配置 JSON；
- 重新执行健康检查；
- 卸载插件；
- 保存工具调用审计摘要。

插件和工具元数据保存在：

```text
backend/data/plugins.db
```

---

## 4. Agent 与插件工具调用

**用户：**

> agent 回复其不能调用添加的插件

**助手：**

已调整 Agent 的工具接入方式：

- 每轮对话动态读取当前启用的插件工具；
- 将这些工具作为 Function Calling 的 `tools` 参数传递给模型；
- 在系统提示中列出当前可用的外部插件工具；
- 明确要求模型在需求匹配时调用工具，而不是声称无法调用插件；
- 启用或停用插件后，工具会立即同步到 Agent 注册表。

VitalSpan 已识别并注册的工具包括：

- `vitalspan_vitalspan_health_check`
- `vitalspan_vitalspan_compose_dashboard`
- `vitalspan_vitalspan_create_dashboard`
- `vitalspan_vitalspan_list_dashboards`
- `vitalspan_vitalspan_list_chart_types`
- `vitalspan_vitalspan_list_layout_templates`
- `vitalspan_vitalspan_publish_artifact`
- `vitalspan_vitalspan_delete_artifact`

---

## 5. 如何调用插件工具

**用户：**

> 怎么调用插件里的工具的

**助手：**

不需要在对话中手动输入完整工具名。启用插件后，直接用自然语言描述目标，Agent 会根据工具描述自动选择并调用对应工具。

示例：

| 对话输入 | 可能调用的工具 |
|---|---|
| 检查 VitalSpan 服务状态 | `vitalspan_vitalspan_health_check` |
| 列出 VitalSpan 里的所有仪表盘 | `vitalspan_vitalspan_list_dashboards` |
| 列出可用的图表类型 | `vitalspan_vitalspan_list_chart_types` |
| 列出可用的数据大屏布局模板 | `vitalspan_vitalspan_list_layout_templates` |
| 创建一个销售数据看板，使用数据大屏布局 | `vitalspan_vitalspan_compose_dashboard` |

对于删除、发布、上传等具有外部副作用的工具，系统会先展示确认卡片；用户确认后才会执行。

---

## 6. 上传 GitHub

**用户：**

> 上传 github `https://github.com/silylucky/BI`

**助手：**

已将实现提交并推送到 GitHub：

- 仓库：<https://github.com/silylucky/BI>
- 分支：`main`
- 提交：`2861cb4 feat: add external JavaScript plugin runtime`

提交内容包含：

- 外部 JavaScript 插件运行时；
- VitalSpan 插件资源；
- 插件管理前端；
- Agent 自动调用插件的接入；
- 插件预检、配置、启停、确认与审计能力。

同时忽略了本地依赖、运行时数据和密钥文件，例如：

```text
node_modules/
backend/data/
backend/.env
.env
```

---

## 7. 调整“添加插件”入口位置

**用户：**

> 放在输入框旁边

**助手：**

将“添加插件”按钮移入输入区域。

**用户：**

> 放在输入框的左侧

**助手：**

最终输入栏顺序调整为：

```text
添加插件 → 输入框 → 发送
```

相关修改位于：

- `frontend/src/App.tsx`：调整输入区按钮的 DOM 顺序；
- `frontend/src/index.css`：将按钮样式改为输入区内的普通 Flex 子元素，并增加移动端样式。

---

## 8. 运行配置（JSON）说明

**用户：**

> 这里的运行配置（JSON）的作用是什么

**助手：**

运行配置用于为**当前插件**保存独立的运行参数。在 Agent 调用该插件工具时，后端会把这份配置传递给插件运行时。

VitalSpan 可使用以下配置：

```json
{
  "vitalspan": {
    "apiBaseUrl": "http://127.0.0.1:8000/api/v1",
    "feAdminUrl": "http://127.0.0.1:5173/admin",
    "username": "admin",
    "password": "changeme",
    "vitalspan_root": "C:\\path\\to\\vitalspan"
  }
}
```

也支持不带 `vitalspan` 外层的形式：

```json
{
  "apiBaseUrl": "http://127.0.0.1:8000/api/v1",
  "feAdminUrl": "http://127.0.0.1:5173/admin",
  "username": "admin",
  "password": "changeme"
}
```

字段说明：

| 字段 | 作用 |
|---|---|
| `apiBaseUrl` | VitalSpan 后端 API 地址 |
| `feAdminUrl` | VitalSpan 管理端页面地址 |
| `username` / `password` | 插件请求 API 时使用的账号信息 |
| `vitalspan_root` | VitalSpan 本地项目根目录，可用于本地完整校验 |

默认配置为 `{}` 时，插件使用自身内置默认值。

> 注意：运行配置保存在本地 SQLite 中；不要将生产环境密钥直接填写到此处。

---

## 9. 当前交付结果

外部插件功能现已具备以下完整流程：

```text
添加插件 → 识别 plugin.json → 发现工具 → 健康检查 → 启用插件
→ Agent 获取工具定义 → 自然语言触发工具调用 → 返回结果或请求高风险确认
```

插件管理入口位于聊天输入框左侧，可用于后续安装和管理其他兼容的本地 JavaScript 插件。

---

## 10. 将 BI Agent 重构为通用插件智能体

**用户：**

> 我要做的是一个可以接入外部插件的通用的智能体，core.py 中的内容是不是应该进行更改

**助手：**

确认原先的 `core.py` 存在 BI 领域耦合，不适合作为通用 Agent 核心，主要包括：

- 类名为 `BiAgent`；
- 内置提示词固定要求进行 SQL 查询、数据分析和图表生成；
- 在核心循环中直接识别 `generate_chart` 与 `query_database`，并生成图表、表格前端事件。

已将其重构为 `GenericAgent`，使核心仅承担以下通用职责：

```text
模型调用 → 工具选择 → 风险确认 → 工具执行 → 工具结果回传模型 → 最终回答
```

改造结果：

- 动态读取 `PluginRegistry` 中的内置工具和外部插件工具；
- 不再假设工具属于 BI 领域；
- 支持通过 `.env` 配置系统提示词与最大工具调用轮次；
- 保留 `BiAgent = GenericAgent` 兼容别名；
- 将图表、表格等 BI 专属展示逻辑移至 `main.py` 的展示适配层。

相关配置项：

```env
AGENT_SYSTEM_PROMPT=你是公司的通用业务助手。优先使用已注册工具完成任务。
AGENT_MAX_TOOL_ITERATIONS=10
```

这两项不写入 `backend/.env` 时仍可正常运行：系统提示词使用 `GenericAgent.DEFAULT_SYSTEM_PROMPT`，最大循环次数默认是 `10`。

---

## 11. BI 可视化、数据库查询与通用核心的职责划分

**用户：**

> agent 中负责数据可视化代码在哪里

**助手：**

数据可视化分为三层：

| 层级 | 位置 | 职责 |
|---|---|---|
| 图表生成 | `backend/plugins/chart_generator.py` | `ChartGeneratorPlugin` 根据数据和图表类型生成图表配置 |
| 结果展示适配 | `backend/main.py` | `adapt_builtin_tool_result()` 将内置 BI 工具结果转换为 `chart`、`table` SSE 事件 |
| 前端渲染 | `frontend/src/components/ChartCard.tsx` | 使用 D3.js 绘制柱状图、折线图、饼图等 |

因此，`backend/agent/core.py` 不再负责具体可视化，只负责通用的模型与工具编排。

**用户：**

> query_database 在哪里

**助手：**

`query_database` 是内置工具，实现在：

```text
backend/plugins/sql_query.py
```

它由 `SqlQueryPlugin` 提供，并在 `backend/main.py` 注册：

```python
registry.register(SqlQueryPlugin()).register(DataAnalysisPlugin()).register(ChartGeneratorPlugin())
```

当模型选择 `query_database` 时，`GenericAgent` 会调用 `PluginRegistry.execute()`，再路由到 `SqlQueryPlugin.execute()` 执行只读 SQL 查询。

**用户：**

> core.py 的功能

**助手：**

`backend/agent/core.py` 是通用 Agent 的编排核心，主要负责：

1. 建立系统提示词与当前工具目录；
2. 将历史消息、用户消息、工具 Schema 发送给模型；
3. 解析模型返回的 Function Calling 请求；
4. 通过 `PluginRegistry` 调用内置或外部插件工具；
5. 对高风险操作发起用户确认；
6. 将工具结果作为 `role="tool"` 回传模型；
7. 通过 SSE 输出工具调用、确认请求、最终回答和会话历史事件。

---

## 12. DeepSeek Function Calling 调用流程

**用户：**

> 是怎么进行调用大模型进行 Function Calling，将聊天上下文和工具 Schema 发给 DeepSeek 的

**助手：**

调用链路如下：

```text
前端 POST /chat
→ main.py 获取会话历史并调用 GenericAgent.chat()
→ core.py 组装 messages 和 tools
→ OpenAI 兼容 SDK 请求 DeepSeek API
→ DeepSeek 返回普通回答或 tool_calls
→ PluginRegistry 执行内置工具或外部插件
→ 工具结果以 role=tool 回传 DeepSeek
→ DeepSeek 生成最终回答
```

DeepSeek 客户端在 `backend/agent/llm_client.py` 中初始化：

```python
llm_client = OpenAI(
    api_key=Config.DEEPSEEK_API_KEY,
    base_url=Config.DEEPSEEK_BASE_URL,
)
```

每个工具继承 `BasePlugin`，并被转换为 OpenAI Function Calling Schema：

```python
{
    "type": "function",
    "function": {
        "name": self.name,
        "description": self.description,
        "parameters": self.parameters,
    },
}
```

随后由通用核心调用模型：

```python
response = llm_client.chat.completions.create(
    model=MODEL,
    messages=messages,
    tools=tool_definitions,
)
```

如果模型返回 `tool_calls`，Agent 执行指定工具，并将结果追加为：

```python
{
    "role": "tool",
    "tool_call_id": tool_call.id,
    "content": json.dumps(result, ensure_ascii=False),
}
```

模型在下一轮读取工具结果，并继续调用工具或生成最终回复。

---

## 13. ReAct 风格循环

**用户：**

> 在 agent 回复过程中有 react 循环进行吗

**助手：**

当前实现有一个有限轮次的 **Function Calling 驱动 ReAct 风格循环**，但不会向前端暴露模型内部思维过程。

```text
用户问题
→ Action：模型选择工具
→ Observation：工具执行并返回结果
→ 再次调用模型
→ 继续行动或输出 Answer
```

循环位于 `GenericAgent.chat()`，最大轮数通过 `AGENT_MAX_TOOL_ITERATIONS` 控制，默认值为 `10`。

这是生产环境更安全的模式：系统保留“行动—观察—再行动”的能力，但只展示工具调用与最终结果，不展示模型内部推理文本。

---

## 14. 外部插件来源与兼容要求

**用户：**

> 我可以从哪里寻找类似的插件

**助手：**

可从以下渠道寻找能力或插件源码：

1. **GitHub**：搜索 `plugin.json agent tool javascript`、`OpenAI function calling tools javascript`、`mcp-server`、`BI dashboard plugin` 等关键词；
2. **npm**：适合寻找数据库、HTTP、文件、OCR、文档处理等能力库，再封装成插件；
3. **MCP Server 生态**：可以通过后续开发 MCP Client 适配器接入；
4. **基于 VitalSpan 模板改造**：复制现有目录结构，替换工具实现，是最快的开发方式。

当前系统可直接安装的插件需要包含 `plugin.json`，并至少支持以下任一工具导出形式：

```javascript
// DeepTalk / VitalSpan Tool
exports.tool = {
  info() { /* 返回名称、描述、参数 */ },
  async run(context, call) { /* 执行 */ }
}
```

```javascript
// 通用协议
exports.manifest = {
  name: "example_tool",
  description: "示例工具",
  parameters: { type: "object", properties: {} }
}

exports.execute = async (args, context) => ({ ok: true })
```

```javascript
// ExecTool 协议
exports.run = async (params, context) => ({ ok: true })
```

安装第三方插件前必须审查来源与源码，因为本地 Node.js 插件可能访问文件系统和网络。

---

## 15. 长任务与工具执行过程展示

**用户：**

> 长任务怎么处理？

**助手：**

当前的 `/chat` SSE 请求适合短任务：模型调用与工具执行在同一个请求中串行完成。创建大屏、批量文件处理、部署、爬取等长任务不应长期占用一次对话请求。

推荐使用“任务化”架构：

```text
用户请求
→ Agent 判断需要长任务
→ 创建 task_id 并持久化
→ Worker 异步执行插件工具
→ 持续写入进度、日志和结果
→ 前端按 task_id 订阅事件
→ 完成后由 Agent 总结结果
```

建议状态：

```text
queued / running / waiting_confirm / succeeded / failed / cancelled
```

建议新增：

```text
backend/tasks/
├─ models.py
├─ store.py
├─ service.py
└─ worker.py
```

轻量单机可采用 SQLite + 独立 Python Worker；生产多实例可采用 Celery 或 Dramatiq 配合 Redis / RabbitMQ。

**用户：**

> 工具执行有没有过程显示？

**助手：**

当前已显示基础过程：模型决定调用工具时，`core.py` 会发送 `tool_call` SSE 事件，前端展示工具名称和调用参数；高风险工具还会展示确认卡片。

当前尚未实现细粒度进度、日志、耗时和取消控制。后续可扩展事件类型：

```json
{ "type": "tool_started", "tool": "xxx" }
{ "type": "tool_progress", "tool": "xxx", "progress": 35, "message": "正在读取数据" }
{ "type": "tool_log", "tool": "xxx", "message": "已完成第 3/10 个文件" }
{ "type": "tool_completed", "tool": "xxx", "duration_ms": 1250 }
{ "type": "tool_failed", "tool": "xxx", "error": "..." }
```

普通同步插件可以先实现：

```text
tool_call → tool_started → tool_completed / tool_failed
```

真正的百分比进度和实时日志需要插件支持流式输出、任务轮询或 Webhook，并与长任务系统结合。

---

## 16. GitHub 分支上传

**用户：**

> 上传 github `https://github.com/silylucky/BI/tree/branch1`

**助手：**

已推送到指定分支：

- 仓库：<https://github.com/silylucky/BI>
- 分支：`branch1`
- 提交：`a89538e feat: generalize agent orchestration`

提交包含：

- 通用 `GenericAgent` 编排重构；
- 可配置系统提示词与最大工具循环次数；
- BI 图表/表格展示适配从核心逻辑拆分；
- “添加插件”移动到输入框左侧；
- 本对话记录文档的初始版本。

---

## 17. Agent Memory 架构

**用户：**

> agent 的 memory 怎么来做

**助手：**

推荐将 Memory 分为四层：

```text
工作记忆：本轮消息、工具调用、工具结果、任务状态
会话记忆：当前 session 的历史消息和滚动摘要
用户长期记忆：稳定偏好、用户事实、明确指令
知识/经验记忆：历史任务结论、插件经验、文档片段
```

`core.py` 不应直接绑定 SQLite、Redis 或向量库，而应通过类似 `MemoryProvider` 的接口获取检索结果与保存会话：

```python
class MemoryProvider:
    def recall(self, user_id: str, session_id: str, query: str) -> list[dict]:
        ...

    def save_session(self, session_id: str, messages: list[dict]) -> None:
        ...
```

每轮对话前注入：

```text
系统规则 + 用户长期记忆 + 相关历史经验 + 会话摘要 + 最近消息 + 当前问题
```

每轮对话结束后：保存消息、必要时更新会话摘要，并仅在用户明确允许或确认时写入长期记忆。

---

## 18. 当前短期记忆与 Redis

**用户：**

> 短期记忆是用的 redis 吗

**助手：**

不是。当前短期记忆在 `backend/agent/session_manager.py` 中使用 Python 内存字典：

```python
self._sessions: Dict[str, List[dict]] = {}
```

它具有以下限制：

- 后端重启后历史消失；
- 多进程或多实例之间不共享；
- 仅保存最近 `MAX_HISTORY_MESSAGES=50` 条消息；
- 无自动过期和跨服务访问能力。

Redis 适合保存短期会话记忆，因为它是独立的内存型键值数据库，支持多实例共享、TTL 自动过期、List/Hash/Stream 等数据结构。

建议 Redis Key 规范：

```text
agent:session:{session_id}
agent:session:{session_id}:summary
agent:task:{task_id}
agent:task:{task_id}:events
```

推荐分工：

| 数据 | 推荐存储 |
|---|---|
| 当前会话、缓存、任务进度、限流、锁 | Redis |
| 插件配置、审计、用户长期记忆 | SQLite / PostgreSQL |

---

## 19. SQLite 长期记忆的规模与检索

**用户：**

> 长期记忆使用 SQLite 数据库保存，如果保存的条数过多，例如有 1 万条的记忆，对 agent 是不是会有影响

**助手：**

SQLite 存储一万条甚至十万条记忆通常没有问题。风险不在数据库容量，而在于不能把全部记忆直接放进模型上下文。

正确流程：

```text
SQLite 保存全部记忆
→ 根据当前问题检索候选
→ 按相关性、重要性、时效性和类别重排
→ 只取 Top 5～8 条注入 Agent
```

建议 `memories` 表保存以下字段：

```text
id / user_id / session_id / memory_key / category / content
importance / status / created_at / updated_at / expires_at
```

其中 `memory_key` 可实现覆盖更新和冲突治理，例如：

```text
preference.dashboard_theme
profile.default_region
```

当新偏好出现时更新同一 `memory_key`，将旧记忆归档；临时记忆则设置 `expires_at`，过期后不再参与检索。

**用户：**

> 怎么做到在 SQLite 数据库的上万条信息中检索到相关的信息

**助手：**

推荐使用“过滤 → 召回 → 重排 → Top-K”流程：

```text
用户问题
→ 按 user_id、status、过期时间、类别过滤
→ FTS5 全文检索或向量检索召回候选
→ 结合重要性、时效性、类别匹配重排
→ 只取前 5～8 条
```

基础索引示例：

```sql
CREATE INDEX idx_memories_scope
ON memories(user_id, status, expires_at);

CREATE INDEX idx_memories_key
ON memories(user_id, memory_key);
```

可使用 SQLite 内置 FTS5 建立全文索引：

```sql
CREATE VIRTUAL TABLE memory_fts USING fts5(
    content,
    memory_id UNINDEXED,
    user_id UNINDEXED,
    tokenize = 'unicode61'
);
```

中文场景建议增加分词与关键词字段；当需要识别“营收”和“销售额”、“驾驶舱”和“数据大屏”等语义关联时，应再加入 Embedding 向量检索。

**用户：**

> SQLite 数据库可以使用 embedding 模型进行向量检索？

**助手：**

可以，但 SQLite 本身不负责生成 Embedding，也默认没有向量近邻检索。推荐组合：

```text
Embedding 模型生成向量
+ SQLite 保存文本和元数据
+ sqlite-vec 扩展保存向量并执行相似度检索
```

示例结构：

```sql
CREATE TABLE memories (
    id TEXT PRIMARY KEY,
    user_id TEXT NOT NULL,
    content TEXT NOT NULL,
    category TEXT NOT NULL,
    importance INTEGER NOT NULL DEFAULT 3,
    status TEXT NOT NULL DEFAULT 'active',
    created_at TEXT NOT NULL,
    updated_at TEXT NOT NULL,
    expires_at TEXT
);

CREATE VIRTUAL TABLE memory_vectors USING vec0(
    memory_id TEXT PRIMARY KEY,
    embedding float[768]
);
```

向量维度必须与实际 Embedding 模型输出保持一致。推荐采用混合检索：

```text
FTS5：精准检索插件名、项目名、业务术语
+ 向量检索：召回语义接近的记忆
→ 合并去重、元数据过滤、重排
→ Top-K 注入 Agent
```

SQLite 适合作为轻量单机 Agent 的长期记忆元数据库；当未来数据量、并发或向量规模显著增长时，再考虑迁移到专用向量数据库或 PostgreSQL + pgvector。

---

## 20. 后续推荐演进顺序

```text
第一阶段：SQLite 会话持久化 + memories 表 + FTS5 + 显式“记住/忘记”能力
第二阶段：滚动会话摘要、记忆去重与冲突覆盖、时间衰减
第三阶段：本地或云端 Embedding + sqlite-vec 混合检索
第四阶段：Redis 短期会话与任务事件流、异步 Worker 长任务系统
```

核心原则：长期记忆全部可保存，但每一轮 Agent 只能检索并注入少量、相关、有效且有权限的记忆。
